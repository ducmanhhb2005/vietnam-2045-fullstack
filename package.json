{
  "name": "vietnam-2045-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack interactive roleplay quiz: Việt Nam 2045 - Bạn sẽ là ai?",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "db:push": "npm run db:push --prefix server",
    "db:seed": "npm run db:seed --prefix server",
    "setup": "npm run install:all && npm run db:push && npm run db:seed",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
